<odoo>

    <!-- Vista de Formulario -->
    <record id="view_evaluacion_desempeno_form" model="ir.ui.view">
        <field name="name">evaluacion.desempeno.form</field>
        <field name="model">evaluacion.desempeno</field>
        <field name="arch" type="xml">
            <form string="Desempeño">
                <sheet>
                    <group>
                        <field name="title"/>
                        <field name="employee_evaluated"/>
                        <field name="comments" widget="text"/>
                        <field name="deadline"/>
                        <field name="state"/>
                        <field name="score"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Vista Kanban -->
    <record id="view_evaluacion_desempeno_kanban" model="ir.ui.view">
        <field name="name">evaluacion.desempeno.kanban</field>
        <field name="model">evaluacion.desempeno</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="title"/>
                <field name="employee_evaluated"/>
                <field name="comments"/>
                <field name="deadline"/>
                <field name="state"/>
                <field name="score"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="o_kanban_record evaluacion_desmpeno_kanban"> <!-- Clase específica --> 
                            <div>
                                <strong>
                                    <span t-esc="record.title.value"/>
                                </strong>
                            </div>

                            <div>
                                <span t-esc="record.employee_evaluated.value and record.employee_evaluated.value[1]"/>
                            </div>

                            <div>
                                <span t-esc="record.comments.value or 'Sin comentarios'"/>
                            </div>

                            <div>
                                <span t-esc="record.deadline.value"/>
                            </div>

                            <div>
                                <span t-esc="record.state.value"/>
                            </div>

                            <div>
                                <span>Puntuación: </span>
                                <span t-esc="record.score.value"/>
                            </div>

                            <div>
                                <!-- Botón para eliminar el registro -->
                                <button
                                    name="unlink"
                                    type="object"
                                    class="btn btn-danger"
                                    string="Eliminar"
                                />
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Acción para abrir las vistas -->
    <record id="action_evaluacion_desempenos" model="ir.actions.act_window">
        <field name="name">Gestión de evaluación de desempeños</field>
        <field name="res_model">evaluacion.desempeno</field>
        <field name="view_mode">kanban,form</field>
        <field name="help" type="html">
            <p>
                Crea y organiza evaluaciones de desempeños realizados a empleados.
            </p>
        </field>
    </record>

    <!-- Menú en la raíz -->
    <menuitem
        id="menu_evaluacion_desempenos_module_root"
        name="Gestión de evaluación de desempeños"
        sequence="10"
    />

    <menuitem
        id="menu_evaluacion_desempenos_module"
        name="Desempeños"
        parent="menu_evaluacion_desempenos_module_root"
        action="action_evaluacion_desempenos"
        sequence="10"
    />

</odoo>
